version: "3.8"
services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./credentials:/app/credentials:ro
      - ./assets:/app/assets:ro
    restart: unless-stopped
